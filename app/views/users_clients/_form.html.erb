<%= form_for(@users_client) do |f| %>
  <% if @users_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_client.errors.count, "error") %> prohibited this users_client from being saved:</h2>

      <ul>
      <% @users_client.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br />
    <%= f.number_field :client_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
