<%- model_class = Message -%>
<div class="page-header">
  <h1>SMS Messages</h1>
</div>

<div>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_message_path,
            :class => 'btn btn-primary', :style => 'color:white' %>
			
<%= link_to("responses", sms_responses_path, class: "btn") %>
</div>
</br>

<%= render :partial => 'messages_grid' %>
         
    
